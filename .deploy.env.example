# Configuração do deploy — copie para .deploy.env e ajuste.
# O arquivo .deploy.env NÃO é versionado (fica só na sua máquina).
SERVER=root@SEU_IP_DO_VPS
SERVER_DIR=/root/site_institucional_suplavia
BRANCH=main
CONTAINER=suplavia-site
URL=https://www.suplavia.com.br
